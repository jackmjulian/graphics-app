{
  "name": "graphics-app",
  "version": "1.0.0",
  "description": "graphics-app for logging all completed freelance work",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run start:client\" \"npm run start:server\"",
    "start:client": "cd client && npm start",
    "start:server": "cd server && npm run dev"
  },
  "author": "Jack Julian",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
